services:
  artisania-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artisania-website
    restart: always
    environment:
      - VIRTUAL_HOST=artisan-ia.fr
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=artisan-ia.fr
      - LETSENCRYPT_EMAIL=victorienbinant@artisan-ia.fr
    volumes:
      - .:/var/www/html          # 🔥 code monté en live depuis ton dossier
      - /var/log/msmtp.log:/var/log/msmtp.log   # (facultatif, pour garder les logs mail)
    networks:
      - artisan_network          # 🔥 reste sur ton réseau principal nginx-proxy

networks:
  artisan_network:
    external: true
